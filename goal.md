So I want to create a script that first checks whether the Raw or compressed directories are there or not?
If not, it creates it.
It reads the file in the raw.
And compresses them Especially their size Using a Node.js package like sharp And puts them in the compress directory.